<p id="notice"><%= notice %></p>

<p>
  <strong>Label:</strong>
  <%= @attribute_layer.label %>
</p>

<p>
  <strong>Palette:</strong>
  <%= @attribute_layer.palette_id %>
</p>

<%= link_to 'Edit', edit_attribute_layer_path(@attribute_layer) %> |
<%= link_to 'Back', attribute_layers_path %>
