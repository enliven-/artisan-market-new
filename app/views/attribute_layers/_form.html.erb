<%= simple_form_for([@palette, @attribute_layer]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :label %>
    <%= f.collection_select(:palette_id, Palette.all, :id, :label) %>    
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
